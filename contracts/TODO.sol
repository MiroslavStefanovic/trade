// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract TODO {
    //dealing with CEFTA only now
    //1. AnexRegistry could be a SC representing CEFTA rules
    //2. Missing SC that would represent contract. Should have signing parties, activity status, and reference to annex
    //3. This SC should be deployed by factory for each contract
    //4. Changes to ANEX should be implemented by 100% quorum of CEFTA members, not an admin, it would look better,
    //since SC would be taking care of this rule, instead of some multisig address
}
